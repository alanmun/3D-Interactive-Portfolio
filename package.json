{
  "homepage": "https://alanmunirji.dev",
  "name": "my-personal-website",
  "version": "2.0.0",
  "scripts": {
    "deploy": "npm run build && node scripts/pull-blogs.js && gh-pages -d dist",
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "pull-blogs": "node scripts/pull-blogs.js"
  },
  "devDependencies": {
    "@types/three": "^0.131.1",
    "gh-pages": "^3.2.3",
    "typescript": "^4.3.2",
    "vite": "^3.2.7"
  },
  "dependencies": {
    "@picocss/pico": "^1.4.3",
    "marked": "^12.0.2",
    "minimatch": "^3.0.5",
    "three": "^0.131.1"
  }
}
